<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">
    <title>Document</title>
</head>
<body>
    <nav>
        <h4>Node MySql</h4>
        <ul>
            <il><a href="/">Login</a></il>
            <il><a href="/register">Register</a></il>
        </ul>
    </nav>

    <div class="container">
        <form action="/auth/login" method="post">
            <h2>會員登入</h2>

            <input type="text" id="username" name="username" placeholder="帳號" required>

            <div class="password-field">
                <input type="password" id="password" name="password" placeholder="密碼" required>
                <button type="button" class="toggle-password" onclick="togglePassword('password', this)">顯示</button>
            </div>

            <button type="submit">登入</button>
            <button type="button" class="secondary-button" onclick="location.href='/register'">註冊會員</button>
        </form>
    </div>

    <script>
        function togglePassword(fieldId, button) {
            const field = document.getElementById(fieldId);
            if (field.type === "password") {
                field.type = "text";
                button.textContent = "隱藏";
            } else {
                field.type = "password";
                button.textContent = "顯示";
            }
        }
    </script>
</body>
</html>